{{#if session.isAuthenticated}}
  <section class="sidebar">
    <aside>
      <ul>
        <li><i class="icon-list"></i>{{#link-to 'index.collections'}}Image Sets{{/link-to}}
          {{outlet 'collections-submenu'}}
        </li>
        <li><i class="icon-grid-1"></i>{{#link-to 'index.statistics'}}Statistics{{/link-to}}</li>
      </ul>
    </aside>
    <section>
      {{outlet}}
    </section>
  </section>
{{else}}
  <section class="intro">
    <span>
      {{#link-to 'signup'}}<button>Sign Up</button>{{/link-to}}
      {{#link-to 'login'}}<button>Login</button>{{/link-to}}
    </span>
  </section>
{{/if}}