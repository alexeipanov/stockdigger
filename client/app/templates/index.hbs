{{#if session.isAuthenticated}}
  <section class="sidebar">
    <aside>
      <ul>
        <li>Dashboard</li>
        <li><i class="icon-list"></i>{{#link-to 'index.collections'}}Image Sets{{/link-to}}
          {{outlet 'collections-submenu'}}
        </li>
        <li><i class="icon-grid-1"></i>Statistics</li>
      </ul>
    </aside>
    <section>
      {{outlet}}
    </section>
  </section>
{{else}}
  <section class="intro">
    <h1>Take the Tour!</h1>
  </section>
{{/if}}